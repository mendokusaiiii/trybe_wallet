const INITIAL_STATE = {
  currencies: [],
  allValueBRL: 0,
  editor: false,
  idToEdit: 0,
};

wallet = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default wallet;
